<Window x:Class="Shapes3DView.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Shapes3DView"
        mc:Ignorable="d"
        xmlns:h="http://helix-toolkit.org/wpf"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <h:HelixViewport3D ZoomExtentsWhenLoaded="True" ItemsSource="{Binding Elements}">
            <h:GridLinesVisual3D MinorDistance="1" MajorDistance="5" Width="100" Length="100"  />
            <h:SunLight />
        </h:HelixViewport3D>
    </Grid>
</Window>
